{
  "name": "journeyverse",
  "version": "1.0.0",
  "description": "Collaboration travel app that facilitates logistics on upcoming trips",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && node server.js",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}